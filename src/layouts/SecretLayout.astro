---
import ChromeLayout from "./ChromeLayout.astro";
import EntryHeading from "../components/primitives/EntryHeading.astro";
import ScrollDecor from "../components/background/ScrollDecor.astro";
import ContentFrameBox from "../components/content-frame/ContentFrameBox.astro";
import "../styles/archive/layout.css";
import "../styles/content/content.css";
import "../styles/secret.css";
import {
  findNavigationItemByPath,
  getArchivePageNextLinkForPath,
  getSectionFromPath,
  topTabs,
} from "../data/navigation.js";

const currentPath = Astro.url.pathname.replace(/\/$/, "") || "/";
const currentSection = getSectionFromPath(currentPath);

const {
  title,
  subtitle,
} = Astro.props;

const navigationItem = findNavigationItemByPath(currentPath);
const sectionTab = topTabs.find((tab) => tab.key === currentSection);
const sectionKicker =
  currentSection === "characters" ? "FILES" : sectionTab?.labelEn ?? currentSection.toUpperCase();
const frameTitle = title ?? navigationItem?.label ?? "Classified File";
const frameSubtitle = subtitle ?? navigationItem?.labelEn ?? "classified file";
const nextPageLink = getArchivePageNextLinkForPath(currentPath);
---

<ChromeLayout
  pageClass={["archive-page", "secret-page", `is-${currentSection}`]}
  surfaceTone="dark"
  heroVariant="secret"
  mobileSidebarIdPrefix="secret-mobile-page-nav"
>
  <ScrollDecor slot="decor" variant="secret" />

  <section class="archive-content-area secret-content-area">
    <EntryHeading kicker={sectionKicker} title={frameTitle} subtitle={frameSubtitle} />

    <ContentFrameBox
      class="secret-text-box"
      variant="simple"
      tone="dark"
      minHeight="320px"
      nextPageLink={nextPageLink}
    >
      <slot />
    </ContentFrameBox>
  </section>
</ChromeLayout>
