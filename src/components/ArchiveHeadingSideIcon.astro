---
import "../styles/archive-heading.css";

const {
  icon,
  iconAlt = "",
  anchorName,
} = Astro.props;
---

<span
  class="archive-heading-side-icon"
  style={anchorName ? `position-anchor: ${anchorName};` : undefined}
  aria-hidden="true"
>
  <img src={icon} alt={iconAlt} />
</span>
